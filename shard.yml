name: dbcharset
version: 0.2.0

authors:
  - Joachim de Groot <jdegroot@web.de>

description: |
  Convert all tables and columns of a MySQL DB to a given charset and collation

dependencies:
  mysql:
    github: crystal-lang/crystal-mysql
  progress:
    github: askn/progress

targets:
  dbcharset:
    main: src/dbcharset.cr

license: MIT
